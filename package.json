{
  "name": "workout-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "biome check src",
    "format": "biome format --write src",
    "test": "bun test"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.0",
    "@types/bun": "latest",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
